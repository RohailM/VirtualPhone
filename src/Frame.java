/*import javax.swing.ImageIcon;
import javax.swing.JFrame;

public class Frame {
	protected boolean is_visible;
	protected boolean is_resizable;
	protected int x_dimension;
	protected int y_dimension;
	protected String title_of_frame;
	protected String icon_of_frame;

	Frame (boolean vis, boolean resize, int x, int y, String title) {
		this.is_visible = vis;
		this.is_resizable = resize;
		this.x_dimension = x;
		this.y_dimension = y;
		this.title_of_frame = title;
	}
	
	public void set_icon_of_frame(JFrame window, String path) {
		this.icon_of_frame = path; 
		ImageIcon icon = new ImageIcon(path);
		window.setIconImage(icon.getImage());
	}
	
	public static void main (String[] args) {
		JFrame screen = new JFrame();
	}
}*/
